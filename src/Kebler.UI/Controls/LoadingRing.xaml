<UserControl
    x:Class="Kebler.UI.Controls.LoadingRing"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local1="clr-namespace:Kebler.UI.CSControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Uc"
    Width="20"
    Height="20"
    mc:Ignorable="d">
    <local1:SprocketControl
        Width="{Binding Width, ElementName=Uc}"
        Height="{Binding Height, ElementName=Uc}"
        Margin="0"
        Padding="0"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        AlphaTicksPercentage="50"
        InnerRadius="0.23"
        Interval="20"
        IsIndeterminate="True"
        LowestAlpha="50"
        OuterRadius=".4"
        TickColor="White"
        TickCount="12"
        TickStyle="Round"
        TickWidth="1" />
</UserControl>